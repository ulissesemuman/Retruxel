<UserControl x:Class="Retruxel.Views.SceneEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BrushSurface}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="240"/>
        </Grid.ColumnDefinitions>

        <!-- ===== TOP BAR ===== -->
        <Border Grid.Row="0" Grid.ColumnSpan="3"
                Background="{StaticResource BrushSurfaceContainerLow}"
                Padding="16,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Scene tabs -->
                <StackPanel x:Name="SceneTabsPanel" Grid.Column="0" Orientation="Horizontal"/>

                <!-- Coordinates display — TextCode: dado técnico -->
                <TextBlock Grid.Column="1"
                           x:Name="TxtCoordinates"
                           Text="X: 0 | Y: 0"
                           Style="{StaticResource TextCode}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button x:Name="BtnGenerateRom"
                            Style="{StaticResource ButtonPrimary}"
                            Padding="16,0" Height="28"
                            Click="GenerateRom_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ===== LEFT — MODULE PALETTE ===== -->
        <Border Grid.Row="1" Grid.Column="0" Grid.RowSpan="2"
                Background="{StaticResource BrushSurfaceContainerLow}"
                Padding="0,8">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>

                    <TextBlock x:Name="ModulePaletteHeader"
                               Style="{StaticResource TextLabelCaps}"
                               Foreground="{StaticResource BrushPrimary}"
                               Margin="12,0,12,8"/>

                    <StackPanel x:Name="ModulePalettePanel"/>

                    <Separator Margin="12,16"/>

                    <!-- ButtonSecondary (roxo): ação utilitária dentro do editor — uso correto -->
                    <Button x:Name="BtnNewScene"
                            Content="+ NEW SCENE"
                            Style="{StaticResource ButtonSecondary}"
                            Margin="12,0"
                            HorizontalAlignment="Stretch"
                            Padding="0,8"
                            Click="NewScene_Click"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- ===== CENTER — CANVAS ===== -->
        <ScrollViewer Grid.Row="1" Grid.Column="1"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto"
                      Background="{StaticResource BrushSurface}">
            <Grid HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  Margin="40">

                <!-- Canvas outer glow border -->
                <Border BorderBrush="{StaticResource BrushGhostBorder}"
                        BorderThickness="1"
                        Padding="1">

                    <!-- Screen area — size set dynamically from target specs -->
                    <Canvas x:Name="SceneCanvas"
                            ClipToBounds="True"
                            MouseMove="Canvas_MouseMove"
                            MouseLeftButtonDown="Canvas_MouseLeftButtonDown"
                            AllowDrop="True"
                            Drop="Canvas_Drop"
                            DragOver="Canvas_DragOver">

                        <!-- Checkerboard pattern via DrawingBrush -->
                        <Canvas.Background>
                            <DrawingBrush TileMode="Tile"
                                          Viewport="0,0,16,16"
                                          ViewportUnits="Absolute">
                                <DrawingBrush.Drawing>
                                    <DrawingGroup>
                                        <GeometryDrawing Brush="#FF0D0D0D">
                                            <GeometryDrawing.Geometry>
                                                <RectangleGeometry Rect="0,0,16,16"/>
                                            </GeometryDrawing.Geometry>
                                        </GeometryDrawing>
                                        <GeometryDrawing Brush="#FF111111">
                                            <GeometryDrawing.Geometry>
                                                <GeometryGroup>
                                                    <RectangleGeometry Rect="0,0,8,8"/>
                                                    <RectangleGeometry Rect="8,8,8,8"/>
                                                </GeometryGroup>
                                            </GeometryDrawing.Geometry>
                                        </GeometryDrawing>
                                    </DrawingGroup>
                                </DrawingBrush.Drawing>
                            </DrawingBrush>
                        </Canvas.Background>

                        <!-- Canvas elements added here dynamically -->

                    </Canvas>
                </Border>

                <!-- Canvas size label — TextCode: dado técnico de dimensão -->
                <TextBlock x:Name="TxtCanvasSize"
                           Style="{StaticResource TextCode}"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Bottom"
                           Margin="0,4,0,-16"/>
            </Grid>
        </ScrollViewer>

        <!-- ===== RIGHT — PROPERTIES ===== -->
        <Border Grid.Row="1" Grid.Column="2" Grid.RowSpan="2"
                Background="{StaticResource BrushSurfaceContainerLow}"
                Padding="16">
            <StackPanel>
                <TextBlock x:Name="TxtPropertiesTitle"
                           Style="{StaticResource TextLabelCaps}"
                           Foreground="{StaticResource BrushPrimary}"
                           Margin="0,0,0,16"/>

                <!-- Empty state -->
                <TextBlock x:Name="TxtNoSelection"
                           Style="{StaticResource TextBody}"
                           TextWrapping="Wrap"
                           Visibility="Visible"/>

                <!-- Properties content — shown when element selected -->
                <StackPanel x:Name="PropertiesPanel"
                            Visibility="Collapsed"/>
            </StackPanel>
        </Border>

        <!-- ===== BOTTOM — EVENTS PANEL ===== -->
        <Border Grid.Row="2" Grid.Column="1"
                Background="{StaticResource BrushSurfaceContainerHigh}"
                Padding="16,12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           x:Name="TxtEventsTitle"
                           Style="{StaticResource TextLabelCaps}"
                           Foreground="{StaticResource BrushPrimary}"
                           Margin="0,0,0,8"/>

                <ScrollViewer Grid.Row="1"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel x:Name="EventsPanel"/>
                </ScrollViewer>
            </Grid>
        </Border>

    </Grid>
</UserControl>
