<UserControl x:Class="Retruxel.Views.Wizard.Steps.ToolStep3Parameters"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBlock Text="Parameters" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,16"/>
        <TextBlock Text="Define input parameters for your tool (optional)." TextWrapping="Wrap" Margin="0,0,0,24"/>

        <!-- Add parameter button -->
        <Button Content="+ Add Parameter" Click="AddParameter_Click" HorizontalAlignment="Left" Margin="0,0,0,16"/>

        <!-- Parameters list -->
        <Label Content="Parameters"/>
        <Border Background="#2D2D30" BorderBrush="#3F3F46" BorderThickness="1" Padding="16" MinHeight="200">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="ParametersListBox">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#252526" BorderBrush="#3F3F46" BorderThickness="1" Padding="12" Margin="0,0,0,8">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="150"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Name -->
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" VerticalAlignment="Center"/>
                                    <TextBox Grid.Row="0" Grid.Column="1" 
                                             Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                                             Margin="0,0,8,0"/>
                                    <Button Grid.Row="0" Grid.Column="2" Grid.RowSpan="4"
                                            Content="✕" 
                                            Width="24" 
                                            Height="24"
                                            VerticalAlignment="Top"
                                            Click="RemoveParameter_Click"
                                            Tag="{Binding}"/>

                                    <!-- Type -->
                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Type:" VerticalAlignment="Center" Margin="0,8,0,0"/>
                                    <ComboBox Grid.Row="1" Grid.Column="1" 
                                              SelectedItem="{Binding Type, UpdateSourceTrigger=PropertyChanged}"
                                              Margin="0,8,8,0">
                                        <ComboBoxItem Content="string"/>
                                        <ComboBoxItem Content="int"/>
                                        <ComboBoxItem Content="bool"/>
                                        <ComboBoxItem Content="file"/>
                                        <ComboBoxItem Content="folder"/>
                                    </ComboBox>

                                    <!-- Description -->
                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Description:" VerticalAlignment="Top" Margin="0,8,0,0"/>
                                    <TextBox Grid.Row="2" Grid.Column="1" 
                                             Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                                             TextWrapping="Wrap"
                                             AcceptsReturn="True"
                                             Height="60"
                                             Margin="0,8,8,0"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Info box -->
        <Border Background="#2D2D30" BorderBrush="#3F3F46" BorderThickness="1" Padding="16" Margin="0,16,0,0">
            <StackPanel>
                <TextBlock Text="ℹ️ Tool Parameters" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,8"/>
                <TextBlock TextWrapping="Wrap" Foreground="#CCCCCC">
                    Parameters define the inputs your tool needs from the user. These will be passed to your Execute() method.
                </TextBlock>
                <TextBlock TextWrapping="Wrap" Foreground="#CCCCCC" Margin="0,8,0,0">
                    Most tools don't need parameters and can be left empty. Add parameters only if your tool requires user input.
                </TextBlock>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
