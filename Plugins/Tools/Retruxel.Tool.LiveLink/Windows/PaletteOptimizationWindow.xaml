<Window x:Class="Retruxel.Tool.LiveLink.Windows.PaletteOptimizationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PALETTE OPTIMIZATION"
        Width="1000" Height="700"
        WindowStartupLocation="CenterOwner"
        Background="#1a1a1a"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Inter"/>
            <Setter Property="Foreground" Value="#e0e0e0"/>
        </Style>
        
        <Style TargetType="Border" x:Key="SectionBorder">
            <Setter Property="Background" Value="#242424"/>
            <Setter Property="Padding" Value="16"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#2a2a2a" Padding="16,12">
            <TextBlock Text="PALETTE OPTIMIZATION PREVIEW" 
                       FontSize="14" 
                       FontWeight="Bold" 
                       FontFamily="Space Grotesk"/>
        </Border>
        
        <!-- Preview Area -->
        <Grid Grid.Row="1" Margin="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Original -->
            <Border Grid.Column="0" Style="{StaticResource SectionBorder}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" 
                               Text="ORIGINAL (SOURCE CONSOLE COLORS)" 
                               FontSize="12" 
                               FontWeight="Bold"
                               Margin="0,0,0,8"/>
                    
                    <Border Grid.Row="1" 
                            Background="#1a1a1a" 
                            BorderBrush="#333333" 
                            BorderThickness="1">
                        <Image x:Name="ImgOriginal" 
                               Stretch="Uniform"
                               RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                    </Border>
                </Grid>
            </Border>
            
            <!-- Optimized -->
            <Border Grid.Column="2" Style="{StaticResource SectionBorder}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" 
                               Text="OPTIMIZED (TARGET PALETTE)" 
                               FontSize="12" 
                               FontWeight="Bold"
                               Margin="0,0,0,8"/>
                    
                    <Border Grid.Row="1" 
                            Background="#1a1a1a" 
                            BorderBrush="#333333" 
                            BorderThickness="1">
                        <Image x:Name="ImgOptimized" 
                               Stretch="Uniform"
                               RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                    </Border>
                </Grid>
            </Border>
        </Grid>
        
        <!-- Diversity Control -->
        <Border Grid.Row="2" Style="{StaticResource SectionBorder}" Margin="16,0">
            <StackPanel>
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" 
                               Text="DIVERSITY" 
                               FontSize="12" 
                               FontWeight="Bold"
                               VerticalAlignment="Center"/>
                    
                    <TextBlock Grid.Column="2" 
                               x:Name="TxtDiversityValue"
                               Text="1.25" 
                               FontSize="12"
                               FontFamily="Consolas"
                               VerticalAlignment="Center"/>
                </Grid>
                
                <Slider x:Name="SliderDiversity"
                        Minimum="75" 
                        Maximum="125" 
                        Value="125"
                        TickFrequency="1"
                        IsSnapToTickEnabled="True"
                        ValueChanged="SliderDiversity_ValueChanged"/>
                
                <Grid Margin="0,4,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" 
                               Text="0.75 (Frequency)" 
                               FontSize="10" 
                               Foreground="#888888"
                               HorizontalAlignment="Left"/>
                    
                    <TextBlock Grid.Column="1" 
                               Text="1.25 (Distance)" 
                               FontSize="10" 
                               Foreground="#888888"
                               HorizontalAlignment="Right"/>
                </Grid>
                
                <!-- Palette Preview -->
                <Border Margin="0,16,0,0"
                        Background="#1a1a1a"
                        BorderBrush="#333333"
                        BorderThickness="1"
                        Padding="8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" 
                                   Text="OPTIMIZED PALETTE" 
                                   FontSize="10" 
                                   FontWeight="Bold"
                                   Margin="0,0,0,8"/>
                        
                        <ItemsControl Grid.Row="1" x:Name="PalettePreview">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Width="32" Height="32" 
                                            Background="{Binding}" 
                                            BorderBrush="#333333" 
                                            BorderThickness="1"
                                            Margin="0,0,4,4"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </Border>
            </StackPanel>
        </Border>
        
        <!-- Buttons -->
        <Border Grid.Row="3" Background="#2a2a2a" Padding="16">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnCancel" 
                        Content="CANCEL" 
                        Width="100" 
                        Height="32"
                        Margin="0,0,8,0"
                        Click="BtnCancel_Click"/>
                
                <Button x:Name="BtnOk" 
                        Content="OK" 
                        Width="100" 
                        Height="32"
                        Click="BtnOk_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
