// @retruxel:module vars
// @retruxel:target sms
// @retruxel:version 1.0.0
// @retruxel:output vars.h

// @retruxel:block header
// Generated by Retruxel — game variables. Do not edit manually.
#ifndef VARS_H
#define VARS_H

#include <stdint.h>

// ── GameVars ──────────────────────────────────────────────────────────────────
// All project-level game variables in a single struct.
// Access anywhere with: g_vars.hp, g_vars.lives, etc.
typedef struct {
{{#each variables}}
    {{cType}} {{variableId}};{{#if label}}  /* {{label}}{{#if minValue != 0 || maxValue != 255}} [{{minValue}}..{{maxValue}}]{{/if}} */{{/if}}
{{/each}}
} GameVars;

extern GameVars g_vars;

// ── Accessors (optional — use g_vars directly for speed) ─────────────────────
{{#each variables}}
#define VARS_GET_{{upper variableId}}()        (g_vars.{{variableId}})
#define VARS_SET_{{upper variableId}}(v)       (g_vars.{{variableId}} = ({{cType}})(v))
#define VARS_CLAMP_{{upper variableId}}()      if (g_vars.{{variableId}} > {{maxValue}}) g_vars.{{variableId}} = {{maxValue}}; \
                                               if (g_vars.{{variableId}} < {{minValue}}) g_vars.{{variableId}} = {{minValue}};
{{/each}}

{{#if hasPersistent}}
// ── Persistence (SRAM) ────────────────────────────────────────────────────────
void vars_save(void);
void vars_load(void);
{{/if}}

#endif // VARS_H
// @retruxel:end
